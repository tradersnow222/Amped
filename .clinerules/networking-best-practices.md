# Rules
- Use **URLSession** with **async/await** for network requests.
- Implement **structured error handling** with `Result<T, Error>`.
- Implement **retry logic** for transient failures.
- Ensure **API request/response validation** to prevent crashes.
- Use **background URL sessions** for long downloads/uploads.
- Secure all network communication with **HTTPS and certificate pinning**.