#!/bin/bash

# This is a dummy script that doesn't modify any files
# It's used to work around duplicate output issues with Info.plist
# Real HealthKit values are included directly in Amped-Info.plist

echo "Running dummy Info.plist script"
echo "Required HealthKit keys are included directly in Amped-Info.plist"

# Exit successfully without modifying any files
exit 0 